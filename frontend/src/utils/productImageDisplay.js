/**
 * Fotos de producto: fondo opaco para PNG con transparencia (evita “tablero” gris)
 * y resizeMode contain para no recortar el contenido.
 */
export const PRODUCT_PHOTO_BG = '#EEEEEE';

export const productImageResizeMode = 'contain';
